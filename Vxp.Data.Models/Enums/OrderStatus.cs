namespace Vxp.Data.Models.Enums
{
    public enum OrderStatus
    {
        Draft = 100,
        New = 200,
        Pending = 300,
        Accepted = 400,
        Declined = 500,
        InFactory = 600,
        Ready = 700,
        Dispatched = 800,
        Delivered = 900,
        Acquired = 1000
    }
}